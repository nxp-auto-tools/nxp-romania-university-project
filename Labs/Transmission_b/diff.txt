diff --git a/teachers/Transmission/src/adc_utils.c b/teachers/Transmission/src/adc_utils.c
index bd5795e..8e42f51 100644
--- a/teachers/Transmission/src/adc_utils.c
+++ b/teachers/Transmission/src/adc_utils.c
@@ -41,6 +41,9 @@ void v_adcInit(adc_instanceConfig_t* adcInstance) {
  * @return type: uint16_t
  */
 void v_scaleAdcValue(uint16_t scaleMax, uint16_t* ui16AdcValue) {
-    /* process the result to get the value in mV */
-    (*ui16AdcValue) = (((*ui16AdcValue) * scaleMax) / ADC_MAX_VALUE);
+//	 TODO 1: Convert ADC value to percentage
+//	 Formula: (current_value * desired_max) / actual_max
+//	 Hint: ADC_MAX_VALUE = 4096 (12 bits)
+
+    (*ui16AdcValue) = -1/*Complete here*/;
 }
diff --git a/teachers/Transmission/src/main.c b/teachers/Transmission/src/main.c
index 6f335ba..db6d58c 100644
--- a/teachers/Transmission/src/main.c
+++ b/teachers/Transmission/src/main.c
@@ -86,9 +86,9 @@ int main(void) {
 
     /* infinite loop */
     while (1) {
-        if (ui8BrakeLevel > 0) {
-        	v_applyBrakes(ui8BrakeLevel, &ui8Acceleration);
-		}
+    	// TODO 6.2: Add brake application here
+    	// Hint: Check if ui8BrakeLevel > 0, then call v_applyBrakes()
+    	/* COMPLETE HERE */
         v_updateSpeed(ui8Acceleration);
         v_updateGear(ui8AccelerationZeroCounter);
         v_updateRPM();
diff --git a/teachers/Transmission/src/motor.c b/teachers/Transmission/src/motor.c
index 3d7cdc8..1253d5f 100644
--- a/teachers/Transmission/src/motor.c
+++ b/teachers/Transmission/src/motor.c
@@ -73,65 +73,63 @@ uint8_t ui8LastGear = 0;
 void v_updateSpeed(uint8_t ui8Acceleration) {
     uint8_t ui8CurrentGear = ui8_getCurrentGear();
 
-    /* convert acceleration (0–100) to a value between 0 and 1 */
-    float acceleration_factor = (float)ui8Acceleration / ACCELERATION_MAX_PERCENT;
+//  TODO 2.1: Convert acceleration (0-100) to factor (0.0-1.0)
+    float acceleration_factor = -1/* COMPLETE HERE */;
 
-    /* adjust acceleration based on gear: lower gears accelerate faster (1.1, 1, 0.9, 0.8, 0.7, 0.6, 0.5) */
-    float gearFactor = GEAR_FACTOR_BASE - (ui8CurrentGear - 1U) * GEAR_FACTOR_STEP;
-    /* make sure gearFactor doesn’t go below 0.2 */
-    if (gearFactor < MIN_GEAR_FACTOR)
-        gearFactor = MIN_GEAR_FACTOR;
+//  Adjust acceleration based on gear: lower gears accelerate faster (1.1, 1, 0.9, 0.8, 0.7, 0.6, 0.5) */
+//  TODO 2.2: Calculate gear factor
+//  Formula: 1.0 - (gear - 1) * 0.1
+//  Result: gear 1 = 1.0, gear 2 = 0.9, ..., gear 6 = 0.5
+    float gearFactor = -1/* COMPLETE HERE */;
 
-    /* combine acceleration and gear effect */
-    acceleration_factor *= gearFactor;
+//  TODO 2.3: Ensure gearFactor doesn't drop below 0.2
+//  Hint: Use MIN_GEAR_FACTOR
+    /* COMPLETE HERE */
+
+
+//  TODO 2.4: Combine acceleration_factor with gearFactor
+    acceleration_factor = -1/* COMPLETE HERE */;
 
     /* add a hysteresis to gear shift in order to prevent gear fluctuations */
     float shiftUpThreshold = (fGearRatios[ui8CurrentGear] + GEAR_CHANGE_THRESHOLD) * 1.5F;
     /* maximum possible speed should be over the shift threshold to be sure we do the gear change */
     float maxPossibleSpeed = ui8GearSpeed[ui8CurrentGear] + shiftUpThreshold;
 
-    /* calculate braking effect: stronger braking at low acceleration */
-    float braking = (BRAKING_DIVISOR / (ui8Acceleration + BRAKING_DIVISOR));
-    if ((ui8Acceleration < (ui8CurrentGear * BRAKING_THRESHOLD_MULTIPLIER)) && (ui8PreviousGear > ui8CurrentGear)){
-        braking += BRAKING_DIVISOR - gearFactor;
-    }
+//  TODO 2.5: Calculate speed increase (addSpeed)
+//  Formula: acceleration_factor * (maxPossibleSpeed - currentSpeed) * smoothing
+    float addSpeed = -1/* COMPLETE HERE */;
 
-    /* increase speed based on acceleration */
-    float addSpeed = acceleration_factor * (maxPossibleSpeed - fSpeed) * ACCELERATION_SMOOTHING;
     if ((addSpeed > ui8CurrentGear)  && (ui8CurrentGear >= ADD_SPEED_LIMIT_GEAR)){
         addSpeed = ui8CurrentGear - ADD_SPEED_LIMIT_OFFSET;
     }
 
-    fSpeed += addSpeed;
+//  TODO 2.6: Add to speed
+    fSpeed = -1/* COMPLETE HERE */;
 
-    /* decrease speed based on braking force */
-    float decreaseSpeed = braking * (fSpeed * BRAKING_FACTOR);
+//  Calculate braking effect: stronger braking at low acceleration
+//  TODO 2.7: Calculate braking (when acceleration is low)
+//  Formula: 1.0 / (acceleration + 1.0)
+//  Hint: Use BRAKING_DIVISOR
+    float braking = -1/* COMPLETE HERE */;
 
-
-    /*This logic helps simulate engine braking or resistance when the vehicle is climbing a hill:
-    In high-speed reduction scenarios, the system limits the braking force to avoid excessive deceleration.
-    In low gears (typically used for climbing), it increases braking force proportionally to the gear to simulate the extra resistance needed.*/
-
-    /*if (decreaseSpeed > 6.0F){
-        decreaseSpeed = ui8CurrentGear;
+    if ((ui8Acceleration < (ui8CurrentGear * BRAKING_THRESHOLD_MULTIPLIER)) && (ui8PreviousGear > ui8CurrentGear)){
+        braking += BRAKING_DIVISOR - gearFactor;
     }
 
-    if ((decreaseSpeed > 1.0F) && (ui8CurrentGear <= 2U) && (ui8CurrentGear != 0)){
-        decreaseSpeed = BRAKING_ADJUSTMENT * ui8CurrentGear;
-    }*/
+//  TODO 2.8: Calculate speed decrease
+//  Formula: braking * currentSpeed * BRAKING_FACTOR
+    float decreaseSpeed = -1/* COMPLETE HERE */;
 
-    fSpeed -= decreaseSpeed;
+//  TODO 2.9: Subtract from speed
+    fSpeed = -1/* COMPLETE HERE */;
 
     /* add a small delay if speed is not zero */
     if (fSpeed != 0.0F) {
         OSIF_TimeDelay(SPEED_DELAY_MS);
     }
 
-    /* make sure speed stays within valid range */
-    if (fSpeed < SPEED_MIN_THRESHOLD)
-        fSpeed = 0.0F;
-    if (fSpeed > MAX_SPEED)
-        fSpeed = MAX_SPEED;
+//  TODO 2.10: Limit speed between 0 and MAX_SPEED (215)
+    /* COMPLETE HERE */
 
     if (ui8CurrentGear != ui8LastGear) {
         if (ui8CurrentGear == MAX_GEAR)
@@ -171,12 +169,16 @@ void v_calculateRPM(uint8_t ui8Acceleration){
     if (ui8CurrentGear == NEUTRAL_GEAR)
         return;
 
-    /* calculate base RPM (Revolutions Per Minute) based on acceleration */
-    rpmBase = IDLE_RPM + (MAX_RPM - IDLE_RPM) * (1.0F - exp(-ui8Acceleration / 20.0F));
+//  TODO 4.1: Calculate base RPM with exponential growth
+//  Formula: IDLE_RPM + (MAX_RPM - IDLE_RPM) * (1.0 - exp(-acceleration/20.0))
+//  Result: at acceleration=0 → IDLE_RPM, at acceleration=100 → approaching MAX_RPM
+    rpmBase = -1/*COMPLETE HERE */;
 
-    /* reduce RPM (Revolutions Per Minute) growth as gear increases */
-    float gearFactor = RPM_SMOOTHING_WEIGHT / (RPM_SMOOTHING_WEIGHT + (ui8CurrentGear - 1) * GEAR_FACTOR_STEP);
-    rpmBase *= gearFactor;
+//  TODO 4.2: Calculate gear factor (RPM grows slower in high gear)
+//  Formula: 1.0 / (1.0 + (gear - 1) * 0.1)
+    float gearFactor = -1/*COMPLETE HERE */;
+//  TODO 4.3: Apply gear factor to rpmBase
+    rpmBase = -1/*COMPLETE HERE */;
 
     /* smoothly update RPM (Revolutions Per Minute) using a weighted average */
     ui16Rpm = ui16Rpm * RPM_SMOOTHING_BASE + rpmBase * RPM_SMOOTHING_LOW;
@@ -187,11 +189,11 @@ void v_calculateRPM(uint8_t ui8Acceleration){
         ui16Rpm = ui16Rpm * RPM_SMOOTHING_HIGH + rpmBase * RPM_SMOOTHING_HIGH_GEAR;
     }
 
-    /* keep RPM (Revolutions Per Minute) within allowed range */
+//  TODO 4.4: Limit RPM between IDLE (1900) and MAX (8000)
     if ((ui16Rpm < IDLE_RPM) && (ui8CurrentGear != NEUTRAL_GEAR))
-        ui16Rpm = IDLE_RPM;
+        ui16Rpm = -1/*COMPLETE HERE */;
     if (ui16Rpm > MAX_RPM)
-        ui16Rpm = MAX_RPM;
+        ui16Rpm = -1/*COMPLETE HERE */;
 }
 
 /* @brief: Updates the engine RPM (Revolutions Per Minute) based on gear changes.
@@ -210,26 +212,32 @@ void v_updateRPM(void){
         /* determine the RPM (Revolutions Per Minute) change factor based on the gear level */
         if (ui8CurrentGear > previousGear) {
             /* shifting up: reduce RPM (Revolutions Per Minute) more in lower gears, less in higher gears (0.57, 0.6, 0.53, 0.46, 0.39, 0.32, 0.25) */
-            rpmChangeFactor = RPM_SHIFT_UP_BASE - (ui8CurrentGear - 1U) * RPM_SHIFT_STEP;
+            // TODO 5.1: Shift UP - calculate RPM reduction factor
+            // Formula: rpm_shift_up_base - (gear - 1) * rpm_shift_step, minimum 0.2
+            // Example: gear 2 → factor 0.53, gear 6 → factor 0.25
+            rpmChangeFactor = -1/* COMPLETE HERE */;
+
             if (rpmChangeFactor < RPM_SHIFT_MIN)
                 rpmChangeFactor = RPM_SHIFT_MIN;
         } else {
             if (ui8CurrentGear == NEUTRAL_GEAR)
                 rpmChangeFactor = 0.0F;
             else {
-                /* shifting down: increase RPM (Revolutions Per Minute) more in lower gears, less in higher gears (1.33, 1.4, 1.47, 1.54, 1.61, 1.68, 1.75) */
-                rpmChangeFactor = RPM_SHIFT_DOWN_BASE + (ui8CurrentGear - 1U) * RPM_SHIFT_STEP;
+            	// TODO 5.2: Shift DOWN - calculate RPM increase factor
+            	// Formula: rpm_shift_down_base + (gear - 1) * rpm_shift_step, maximum 2.0
+                rpmChangeFactor = -1/* COMPLETE HERE */;
                 if (rpmChangeFactor > RPM_SHIFT_DOWN_MAX)
                     rpmChangeFactor = RPM_SHIFT_DOWN_MAX;
             }
         }
-        ui16Rpm = ui16Rpm * rpmChangeFactor;
+        // TODO 5.3: Apply factor to RPM
+        ui16Rpm = -1/* COMPLETE HERE */;
 
-        /* clamp RPM (Revolutions Per Minute) within valid range */
+        // TODO 5.4: Re-limit RPM
         if ((ui16Rpm < IDLE_RPM) && (ui8CurrentGear != NEUTRAL_GEAR))
-            ui16Rpm = IDLE_RPM;
+            ui16Rpm = -1/* COMPLETE HERE */;
         if (ui16Rpm > MAX_RPM)
-            ui16Rpm = MAX_RPM;
+            ui16Rpm = -1/* COMPLETE HERE */;
     }
 }
 
@@ -318,7 +326,6 @@ void v_motorSelfTest(uint8_t ui8Gear, ftm_instanceConfig_t* ftmMotorInstance) {
 
 /* @brief: Applies brakes to smoothly but quickly decelerate the motor. */
 void v_applyBrakes(uint8_t ui8BrakeLevel, uint8_t * acceleration) {
-    float decayFactor = 1.0F - (ui8BrakeLevel * 0.01F);
-    *acceleration *= decayFactor;
-    if(*acceleration  < 15) *acceleration = 0;
+	//TODO 6.1
+	/* IMPLEMENT COMPLETE FUNCTION HERE */
 }
diff --git a/teachers/Transmission/src/servo.c b/teachers/Transmission/src/servo.c
index ccdde59..7d1c499 100644
--- a/teachers/Transmission/src/servo.c
+++ b/teachers/Transmission/src/servo.c
@@ -105,20 +105,28 @@ void v_updateGear(uint8_t ui8AccelerationZeroCounter) {
     ui8PreviousGear = ui8CurrentGear;
     float fSpeed = f_getSpeed();
 
-    uint8_t *ui8GearSpeed = ui8_getGearSpeedArray();
-    /* shift from neutral to first gear if speed > 0 and acceleration is active */
-    if ((fSpeed > 0.0F) && (ui8CurrentGear == NEUTRAL_GEAR) && (!ui8AccelerationZeroCounter)) {
+    uint8_t *ui8GearSpeed = ui8_getGearSpeedArray();// {3, 20, 40, 70, 110, 160, 200}
+
+//  TODO 3.1: Shift from NEUTRAL (0) to GEAR 1
+//  Conditions: speed > 0 AND gear == 0 AND acceleration active (ui8AccelerationZeroCounter == 0)
+    if (-1/* COMPLETE HERE */) {
         ui8CurrentGear++;
-    /* shift from first gear to neutral if speed is low and acceleration has been zero for a while */
-    } else if ((fSpeed <= ui8GearSpeed[ui8CurrentGear] - GEAR_CHANGE_THRESHOLD) &&
-            (ui8CurrentGear == 1U) &&
-            (ui8AccelerationZeroCounter > ACC_ZERO_COUNTER_THRESHOLD)) {
+    }
+//  TODO 3.2: Shift from GEAR 1 to NEUTRAL
+//  Conditions: speed <= threshold AND gear == 1 AND no acceleration for a while (counter > 10)
+//  Hint: threshold = ui8GearSpeed[ui8CurrentGear] - GEAR_CHANGE_THRESHOLD
+    else if (-1/* COMPLETE HERE */){
        ui8CurrentGear--;
-    /* shift up if speed is high enough and we are not in the highest gear */
-    } else if ((fSpeed > ui8GearSpeed[ui8CurrentGear] + GEAR_CHANGE_THRESHOLD) && (ui8CurrentGear < MAX_GEAR) && (ui8CurrentGear != NEUTRAL_GEAR)) {
+    }
+//  TODO 3.3: Shift UP (2→3, 3→4, etc.)
+//  Conditions: speed > current threshold AND gear < max_gear AND gear != neutral_gear
+//  Hint:
+    else if (-1/* COMPLETE HERE */) {
         ui8CurrentGear++;
-    /* shift down if speed drops below the previous gear’s speed and we are not in the lowest gear */
-    } else if (fSpeed <= ui8GearSpeed[ui8CurrentGear - 1U] && ui8CurrentGear > MIN_GEAR) {
+    }
+//  TODO 3.4: Shift DOWN (6→5, 5→4, etc.)
+//  Conditions: speed <= previous gear threshold AND gear > 1
+    else if (-1/* COMPLETE HERE */) {
         ui8CurrentGear--;
     }
 }
